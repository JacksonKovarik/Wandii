Merged project notes

Base used:
- Main branch snapshot for the fuller UI/features.
- Databaseworks branch pieces for Supabase auth, trip draft state, trip creation, trip storage, and trip list loading.

What was merged:
- Supabase auth context + client setup
- Sign in / sign up screens
- Trip draft context
- Add-trip flow persistence across steps
- Cover photo upload + trip creation into Supabase
- Upcoming / Past trips now load from Supabase
- Trip detail pages keep the richer main-branch UI, but now hydrate from the stored trip row so newly created trips can open correctly
- Chat screen from the databaseworks branch was kept and wired into trip detail tabs

Checks completed:
- npm install
- expo lint (passes)
- TypeScript check with `npx tsc --noEmit` (passes)

Notes:
- node_modules is intentionally not included in the zip.
- Run `npm install` after unzipping on your machine, then start with `npx expo start`.
