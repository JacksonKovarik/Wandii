Patch 2026-03-25B

Fixed Wandii trip schema integration against the user's actual Supabase tables.

Changes:
- Trips now uses the real column names from public.Trips:
  - creator_id
  - trip_name
  - start_date
  - end_date
  - cover_photo_url
  - target_budget
  - vibe
- Trip reads now query Trips.creator_id instead of old user_id/userId columns.
- Trip normalization now maps creator_id/trip_name/target_budget to the UI fields the app expects.
- Trip creation now optionally creates a cached_destinations row and Trip_Destinations link so destination text can still show in the UI.
- Trip creation now optionally adds the creator to Trip_Members.
- Trip reads now load destination labels from Trip_Destinations + cached_destinations.
- Trip deletion now cleans up Trip_Destinations and Trip_Members rows before deleting the trip.

Based on schema provided by the user.
